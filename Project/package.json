{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "author": "Dhrushit Raval",
  "license": "ISC",
  "dependencies": {
    "jstreemap": "^1.28.2"
  },
  "devDependencies": {
    "typescript": "^4.4.4"
  }
}
